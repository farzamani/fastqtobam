# Reference

Place the reference FASTA in this directory as:

- `hs1.fa`

The workflow will build the required `bwa-mem2` index files automatically if they are missing.

Other reference FASTA files can also be used, but the workflow configuration and related paths should be updated to match.
